# Gofer local runtime configuration
#
# Keep real credentials out of this file. Gofer integrations should use the
# host CLI's login/session flow, an OS keychain, or your CI secret store.

LOG_LEVEL=info

# Orchestrator defaults
SPECS_DIR=.specify/specs
MAX_RETRIES=3
