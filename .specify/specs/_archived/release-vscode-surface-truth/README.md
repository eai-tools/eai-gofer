# Archived Release Spec: VS Code Surface Truth

This directory preserves the historical archived location for the
`release-vscode-surface-truth` release-oriented work item.

The active cleanup and surface-parity follow-up lives in
`030-vscode-surface-truth-cleanup`, which remains archived alongside this
compatibility marker so command-generation parity checks can confirm release
specs are not restored to the active top-level specs directory.
