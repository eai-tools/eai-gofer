{
  "mcp": {
    "servers": {
      "gofer": {
        "command": "node",
        "args": [
          "${env:GOFER_LANGUAGE_SERVER_PATH}"
        ],
        "env": {
          "ANTHROPIC_API_KEY": "${env:ANTHROPIC_API_KEY}"
        },
        "description": "Gofer - Spec-driven development orchestrator"
      }
    }
  }
}
