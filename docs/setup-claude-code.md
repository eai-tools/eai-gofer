# Claude Code Setup

Claude Code setup is now covered in one place: [CLI Support](cli-support.md).

Run **Gofer: Initialize Repository** in VS Code, then use Gofer slash commands
from Claude Code, for example:

```text
/0_business_scenario Add user authentication
```
